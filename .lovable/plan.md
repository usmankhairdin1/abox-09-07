# ABox Phase 56 / Batch B9 — Complete Screens / Bulk Application Import (PLAN ONLY)

B8 is closed and verified. B9 is already specified by the governed extraction
(`tools/figma-plugin/tokens-b9.js`, generated by `extract-b9.mjs`) and a complete
builder section already exists in `tools/figma-plugin/plugin.js` (lines 5453–5900).
This plan approves that existing scope unchanged and lists the pre-run corrections
required before the first real Figma run.

## Approved scope (from the governed contract, not invented)

Page: **05 Screens** only. Operation: "Create All Screens" / "Verify All Screens".
Model: one-bulk-import, standalone editable design/prototype artifact, no permanent sync.

- **179 top-level FRAMEs**, named `ABox/Screen/...` and `ABox/ScreenState/...`, in the
  deterministic order and at the exact x/y in `ABOX_B9.placement` (179 entries),
  canonical width 1440, gaps 1560 × 1200.
  Arithmetic of record: 142 content route files − 2 internal design references −
  12 workforce hosts − 1 quote host − 1 agent-profile host + 35 M06 screens +
  10 M08 screens + 6 quote UX state frames + 2 source-backed visual-state frames = 179.
- **Each frame has exactly five regions, in this order:**
  `metadata/source-and-identity`, `shell-reference`, `foundations-and-states`,
  `prototype-links`, `runtime-boundaries`.
- **Reuse of existing assets only** — B7 shell instances (Internal / Marketplace /
  Member), B6 pattern instances (KpiRow, ModuleTabBar, WizardStepper), B4/B5
  component instances, B3 styles, B1/B2 variables. Per the contract counts:
  0 new variables, styles, components, component sets, patterns, properties.
- **682 native prototype reactions** (Category A only), written with
  `setReactionsAsync`, one reaction per hotspot node, each carrying its interaction
  id, signature, target key, trigger, action, navigation and transition as plugin data.
- **Categories B (135), C (102), D (3)** are reported as counts in the
  `runtime-boundaries` region — never simulated.

Source traceability: every frame stores `aboxBatch=B9`, kind, name, key, signature,
sorted source list, route and screen id as plugin data; the `metadata` region prints
title, id, route, source type, purpose and the full source file list.

## Dependencies on B0–B8

- B0: the seven pages in original order; 00 Foundations must stay empty; 06 Documentation
  empty or holding exactly the approved B10 frames.
- B1 (9 collections / 200 vars), B2 (19 vars), B3 (79 styles) — read-only, verified unchanged.
- B4/B5 (11 sets, 3 standalone, 59 physical nodes, 19 non-variant properties), B6 (3/4),
  B7 (3 shells / 4 physical nodes), B8 (5 experience frames) — all resolved before any
  B9 write and asserted unchanged by `verifyB9()`.

## Pre-run plugin corrections required (dynamic-page async + rollback)

The B9 section predates the async conversions applied in B7/B8 and will fail at runtime
as written. The smallest corrections, mirroring the approved B8 fixes:

1. **Async frame creation.** Add `b9Frame(name, opts, index)` — an async twin of
   `b7FrameLegacy` using `setFillStyleIdAsync` / `setStrokeStyleIdAsync` /
   `setEffectStyleIdAsync` (or reuse the existing `b8Frame`). Switch the 8
   `b7FrameLegacy(...)` call sites inside the B9 region builders (5530, 5539, 5552,
   5554, 5558, 5561, 5573, 5581, 5598) to `await`. `b7FrameLegacy` itself stays untouched
   for B10.
2. **Async root setters** in `b9BuildFrame` (5617–5618): `await root.setFillStyleIdAsync(...)`,
   `await root.setStrokeStyleIdAsync(...)`.
3. **`b9Instance` must be async** — it returns `b8CreateInstance(...)`, which is now a
   Promise. Make it `async` and `await` it at the three call sites (5542, 5555, 5559),
   serialising the two loops so instances append in deterministic order.
4. **`verifyB9` line 5862**: `const mains = b8InstanceMainNames(frame);` is un-awaited —
   the identical bug already fixed in `verifyB8`. Add `await`.
5. **Guarded rollback**: wrap the create path in the handler as
   `await b9Guarded(b9Page(), "Batch B9 — screens", ensureB9Screens)` (mirroring
   `b8Guarded`), so a mid-run failure removes only nodes created by that run.
   Verification stays outside the guard.

No other B9 logic, no signature, region, placement, reaction or dependency contract
changes.

## Explicitly deferred / out of scope

No new variables, styles, components, component sets, patterns, component properties or
variant axes. No responsive variants, no image fills or screenshots, no HTML/embeds, no
Category B/C/D prototype simulation, no edits to B0–B8 objects, no B10 work, no publish,
no `src/**` changes (read-only inspection only), no changes to `extract-b9.mjs` or
`tokens-b9.js`.

## Validation

Offline (this repo): `node --check tools/figma-plugin/plugin.js`, `node build.mjs`,
`node --check tools/figma-plugin/code.js`, `git diff --stat -- src/` empty, and a diff
review confirming zero hunks outside the B9 range plus the b9-run handler.

Real Figma (user-run, after copying `code.js` to the local plugin folder):
Create All Screens → expect 179 frames created, 682 reactions created, all B0–B8
assertions PASS → Verify All Screens → Create All Screens again.

## Idempotency

Run 2 must create nothing: `b9FindFrame` matches by name (duplicates STOP),
`b9AssertReusable` compares plugin data (batch/kind/name/key/signature/sorted sources)
and the five ordered region names, and every source-backed hotspot must be present;
`b9EnsureReaction` reuses an existing matching reaction and STOPs on a conflicting one
rather than overwriting. Bookkeeping asserts frames created = 0 and reactions created = 0
on run 2. Any unapproved object on 05 Screens STOPs the run; nothing is ever deleted,
renamed or overwritten.

## Confirmations

B0–B8 plugin logic and Figma structures remain unchanged; `src/**` is read-only; no
implementation, no file edits and no Figma mutation are performed in this planning step.

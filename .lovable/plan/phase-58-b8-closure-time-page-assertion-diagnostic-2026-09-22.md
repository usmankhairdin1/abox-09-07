# Phase 58 — B8 closure-time page assertion (diagnostic)

## Exact source location

`tools/figma-plugin/plugin.js`, inside `verifyB8()` (function starts at line 5466), page-ownership block at lines **5475–5492**:

```text
5475  // "06 Documentation" may hold exactly the approved B10 frames …
5476  const b8Writable = [B4_PAGE, B6_PAGE, B7_PAGE, B8_PAGE];
5477  const b8PageEvidence = [];
5478  for (const p of figma.root.children) {
5479    if (b8Writable.indexOf(p.name) !== -1) continue;
5480    if (p.name === B10_PAGE) { … continue when empty or exactly b10ApprovedNames() }
5484    for (const child of p.children) {
5485      if (b8PageEvidence.length >= 40) break;
5486      b8PageEvidence.push("  " + p.name + " › " + child.name + " (" + child.type + ")  id=" + child.id);
5487    }
5488  }
5489  add(b8PageEvidence.length === 0,
5491      "00 Foundations and 05 Screens remain empty; 06 Documentation may hold the approved B10 frames — unexpected nodes: " + b8PageEvidence.length);
```

## Root cause

Identical class to the stale batch-local page assertions already corrected in B0, B4, B5, B6 and B7. The skip list `b8Writable` covers 01 Components, 02 Patterns, 03 Shells and 04 Experiences, plus a hand-written 06 Documentation special case — but **05 Screens has no allowance** and is still required to be empty.

At closure time 05 Screens legitimately holds 179 approved B9 frames. 00 Foundations is empty and 06 Documentation passes its special case, so every offender comes from 05 Screens. The inner loop `break`s at `b8PageEvidence.length >= 40` and the label prints the buffer length, so the reported **40** is the evidence cap, not a true count — all 40 listed lines are approved B9 screen frames.

Secondary defects in the same block: the four skipped pages get no identity check at all, so a stray or renamed node there is invisible to B8, and the printed count is the buffer size rather than the real offender total.

The canonical helper `aboxApprovedPageTopLevel()` (plugin.js 547) already contains the complete B0–B10 map: 00 Foundations `[]`, 01 Components `ABOX_B4.sets + ABOX_B4.components`, 02 Patterns `ABOX_B6.patterns`, 03 Shells `ABOX_B7.shells`, 04 Experiences `ABOX_B8.experiences`, 05 Screens `ABOX_B9.screens`, 06 Documentation `ABOX_B10.documents`, each behind a `typeof` guard. No new inventory source is needed, and the B10 special case is redundant.

All B8-specific checks pass and are untouched by this diagnosis: the five approved experience frames, deterministic order and placement, 1440px canonical width, four approved child regions, shell/pattern/component instance resolution, source traceability, limitations/deferred foundations, the zero-new-object assertions and idempotent creation counts.

## Exact minimal correction

Replace **only** lines 5475–5492 with the canonical allowlist check — no hard-coded writable skip list, no B10 special case, no generic page allowance:

- `const b8ApprovedTopLevel = aboxApprovedPageTopLevel();`
- `const b8PageEvidence = []; let b8UnexpectedTopLevel = 0;`
- For every page in `figma.root.children`, for every top-level child: if that page's approved map has the child's name, `continue`; otherwise `b8UnexpectedTopLevel += 1`, and when `b8PageEvidence.length < 40` push `"  " + p.name + " › " + child.name + " (" + child.type + ")  id=" + child.id` (`continue`, never `break`, so the counter stays true past the cap).
- `add(b8UnexpectedTopLevel === 0, "each page holds only its approved B0–B10 top-level inventory (00 Foundations must stay empty); unexpected nodes: " + b8UnexpectedTopLevel);`

Lines 5493–5497 (the existing `B8 PAGE EVIDENCE` printing block) stay byte-identical and are fed by the same array. Lines 5473–5474 (wantPages + B0 order assertion) and everything from 5498 onward stay byte-identical.

## Expected post-fix B8 behaviour

- 00 Foundations must be strictly empty; any node there FAILs and is named.
- 01–06 accept exactly their approved B4/B5, B6, B7, B8, B9, B10 top-level names; anything renamed, extra or stray FAILs, is listed with page, name, type and id (first 40), and the label reports the true offender count.
- All B8-specific inventory, geometry, region, instance-resolution, traceability, zero-new-object and idempotency checks unchanged; expected result **B8 PASSED**.

## Files that would change

- `tools/figma-plugin/plugin.js` — the single block at 5475–5492.
- `tools/figma-plugin/code.js` — regenerated by `node tools/figma-plugin/build.mjs`.

Validation: `node --check tools/figma-plugin/plugin.js`, `node tools/figma-plugin/build.mjs`, `node --check tools/figma-plugin/code.js`, `git diff --stat -- src/` empty.

## Confirmation

Verification/evidence logic only. Zero Figma objects created, modified, moved, renamed or deleted; no creation/build logic touched; B0–B7 and B9/B10 verifier logic untouched; no `src/**`, `roadmap.md` or publishing changes; no B11.

# Phase 58 — Verify B0 second closure-time correction (PLAN ONLY)

## Confirmed diagnosis

The real-Figma inventory is consistent with the governed B1–B10 outputs. The two failures are caused by incomplete exact-name allowlists inside `verifyLibraryPages()`, not by unexpected Figma objects.

### B2 collection exclusion

File: `tools/figma-plugin/plugin.js`, function `verifyLibraryPages()`, current lines 439–455.

The verifier builds its collection allowlist only from:

```js
const b1Names = ABOX_B1.collections.map((c) => c.name);
```

It then treats every collection whose name is absent from `b1Names` as unexpected. This necessarily rejects the tenth approved collection created by B2.

The canonical B2 identity is not inferred from the displayed failure text. It is declared by the generated B2 contract in `tools/figma-plugin/tokens-b2.js`:

```js
ABOX_B2.collection.name // "ABox/Typography"
```

Its authoritative source is `tools/figma-plugin/extract-b2.mjs`, which emits:

```js
collection: { name: "ABox/Typography", modes: ["Light", "Dark"] }
```

Therefore the current B0 closure verifier is incomplete because it consults `ABOX_B1.collections` but never adds `ABOX_B2.collection.name`.

### B6 pattern exclusion

File: `tools/figma-plugin/plugin.js`, function `verifyLibraryPages()`, current lines 458–494.

The verifier currently builds approved component identities from only:

- `ABOX_B4.sets[*].name`
- `ABOX_B4.components[*].name`
- `ABOX_B7.shells[*].name`

There is no `ABOX_B6` branch. Consequently:

- `ABox/Pattern/KpiRow` fails because its COMPONENT_SET name is absent;
- its `columns=4` and `columns=3` COMPONENT children fail because the existing variant-child exemption applies only when the parent COMPONENT_SET is approved;
- `ABox/Pattern/ModuleTabBar` and `ABox/Pattern/WizardStepper` fail because their standalone component names are absent.

The canonical B6 identities are declared by the generated B6 contract in `tools/figma-plugin/tokens-b6.js`:

```js
ABOX_B6.patterns[*].name
ABOX_B6.patterns[*].kind // "SET" or "COMPONENT"
```

That contract contains exactly the three governed pattern identities and is generated by `tools/figma-plugin/extract-b6.mjs` from its `patterns` array. The variant labels are not separate allowlist identities: the existing parent-set rule correctly admits them once `ABox/Pattern/KpiRow` is approved from `ABOX_B6.patterns`.

B5 does not require a new name source in this check: its governed work extends the B4 component inventory, whose canonical set and standalone names remain `ABOX_B4.sets` and `ABOX_B4.components`. Thus the exact closure inventory is represented by B4 names (including the B5-mutated inventory), B6 pattern names, and B7 shell names.

## Narrow correction scope

Change only the verification/evidence section inside `verifyLibraryPages()` in `tools/figma-plugin/plugin.js`:

1. Build the approved collection-name set from the union of:
   - `ABOX_B1.collections[*].name`
   - `ABOX_B2.collection.name`
2. Continue collecting every local variable collection whose exact name is outside that union, print each unexpected name, and FAIL when any exists.
3. Update only that check's wording from “approved B1 inventory” to “approved B1/B2 inventory” so the evidence matches the enforced contract.
4. Extend `approvedComponents` with the exact names from `ABOX_B6.patterns[*].name`, alongside the existing B4 and B7 sources.
5. Retain the existing approved-parent rule for COMPONENT children of an approved COMPONENT_SET. This admits the two governed KpiRow variants without allowing arbitrary standalone component names.
6. Continue printing every unexpected COMPONENT/COMPONENT_SET as `page › name (type) id=…` and FAIL when any node is outside the exact B4/B5/B6/B7 inventory.
7. Update only that component check's wording from “B4/B5/B7” to “B4/B5/B6/B7”.

No generic collection or component allowance will be introduced. Exact contract names remain the boundary.

## Protected logic

Keep all existing B0 page and baseline assertions byte-identical:

- library filename;
- seven pages exactly once;
- native PAGE nodes;
- page indices 0..6 in declared order;
- no extra pages;
- no image fills.

Keep the existing B3 text-style and effect-style exact-name checks unchanged. Keep the B0 inventory/evidence block, traversal, result handling, and all B1–B10 creation contracts unchanged except for the two necessary evidence labels above.

## Files and effects

Implementation, once separately approved, touches only:

- `tools/figma-plugin/plugin.js` — `verifyLibraryPages()` allowlist/evidence logic;
- regenerated `tools/figma-plugin/code.js` — build output of that verifier-only change.

It does not edit token or extractor contracts, builders, other verifiers, `src/**`, or Figma content. It creates, renames, moves, deletes, or modifies **zero Figma objects**. There is no B11 and no publish.

## Validation after a later implementation approval

- Syntax-check `plugin.js`.
- Rebuild `code.js` and syntax-check it.
- Confirm `src/**` has no diff.
- In Figma Desktop, run Verify B0 only and confirm:
  - the ten exact B1+B2 collections pass;
  - all approved B4/B5/B6/B7 component nodes pass;
  - all original B0 page/baseline checks continue to pass;
  - the verifier remains read-only and reports `RESULT: B0 PASSED`.
- Resume the Phase 58 B1→B10 verification sweep only after that real-Figma PASS.

## Planning-only confirmation

No correction is implemented in this phase response. This plan changes no plugin source, generated plugin bundle, Figma object, creation contract, application source, or publication state.
